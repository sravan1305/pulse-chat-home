// Single source of truth for the demo's "now" — the synthetic data covers 2025,
// so we anchor the app to a representative day rather than the wall clock.
export const DEMO_TODAY = "2025-12-31"; // YYYY-MM-DD
export const DEMO_NOW_HOUR = 18; // synthetic "current hour" — dark, heating + EV active

export const DEFAULT_HOUSEHOLD_ID = "HH-1001";
